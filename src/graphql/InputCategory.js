'use strict';

const SHORT_TEXT = 'SHORT_TEXT';
const LONG_TEXT = 'LONG_TEXT';
const INT = 'INT';
const FLOAT = 'FLOAT';
const DATETIME = 'DATETIME';
const BOOLEAN = 'BOOLEAN';

const DEFAULT = SHORT_TEXT;

const name = 'InputCategory';


module.exports = {

    name,
    
    SHORT_TEXT,
    LONG_TEXT,
    INT,
    FLOAT,
    DATETIME,
    BOOLEAN,

    DEFAULT

};
